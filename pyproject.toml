[project]
name = "case-willbank"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dbt-core~=1.9",
    "dbt-duckdb==1.8.0",
    "dbt-metricflow>=0.8.2",
    "duckdb>=1.3.2",
]
